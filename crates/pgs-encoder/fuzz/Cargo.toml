[workspace]

[package]
name = "pgs-encoder-fuzz"
version = "0.0.0"
edition = "2021"
publish = false

[package.metadata]
cargo-fuzz = true

[dependencies]
pgs-encoder = { path = ".." }
libfuzzer-sys = "0.4"

[[bin]]
name = "decode_pgs"
path = "fuzz_targets/decode_pgs.rs"
