# PGS/SUP Spec Compliance Deep Audit

**Status**: IN PROGRESS — PotPlayer still crashes despite multiple fixes

## Root Cause Hypothesis (unverified)

We have been guessing at the root cause. The correct approach is:
1. Find a KNOWN-GOOD SUP file (from BDSup2Sub, SUBTools, or the repo's own test fixture)
2. Compare its binary structure against ours byte-by-byte
3. Find the EXACT byte offset where our output diverges
4. Fix the encoder at that exact point

## Investigation Plan

### Phase 1: Generate Reference SUP
- [ ] Use the existing `tests/fixtures/simple.sup` as reference (generated by old code, known to work in some players)
- [ ] Generate a new SUP from `tests/fixtures/simple.ass` with current code
- [ ] Compare byte-by-byte: segment headers, payload sizes, palette entries, ODS flags

### Phase 2: Binary Diff Analysis
- [ ] Write a Python script that parses both SUP files and compares every segment field
- [ ] Focus on: PCS (palette_update byte), PDS (palette entry byte order), ODS (flags byte, total_size field), WDS (window dimensions)
- [ ] Report every byte difference between reference and new

### Phase 3: Targeted Fix
- [ ] Apply the single most critical difference as a fix
- [ ] Verify with test
- [ ] If still crashes, apply next difference

### Known Differences (from previous analysis)
- ODS payload: old writes width(2)+height(2)+rle_len(4) = 10 bytes; new writes total_size(3)+width(2)+height(2) = 7 bytes
- ODS flags: old = 0x80; new = 0xC0
- PDS byte order: old = Y,Cb,Cr; new = Y,Cr,Cb
- palette_update: old = !palette_changed; new = true always

## Key Question for User
**PotPlayer 崩溃发生在什么时候？**
- 导入 SUP 文件时立即崩溃？
- 播放到特定时间点时崩溃？
- 拖动进度条时崩溃？

这个信息对定位问题至关重要。
